# Test Environment Variables
NODE_ENV=test
PORT=3001

# Test Database
DB_NAME=agenziecase_test
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# JWT
JWT_SECRET=test-secret-key-for-testing-only-do-not-use-in-production
JWT_EXPIRE=1h

# API Keys (use mock/test keys)
ANTHROPIC_API_KEY=test-key
GOOGLE_CLOUD_KEY=test-key

# Email (disable in tests)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASSWORD=
