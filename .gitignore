node_modules
# Environment files (SECURITY: Never commit credentials)
server/.env
.env.local
.env.production  # Contains production credentials - never commit this file
*.env

# Files with potential credentials (SECURITY RISK)
**/test-email.js
**/config-email-ovh.sh

# Temporary files
temp-*
*.tar.gz

# Claude Code workspace
.claude/

# Development/fix scripts
fix-localhost-urls.cjs
nginx-temp-http.conf
server/create-admin.js

# Nginx configs (but keep SSL configs)
temp-*.conf
nginx.conf

# SSH keys (already there but just to be sure)
*.pem
*.key
 agenziecase-complete.tar.gz
.env.production
